{
  "name": "asdesigns-monorepo",
  "private": true,
  "workspaces": ["apps/*", "shared"],
  "scripts": {
    "build": "npm run -w shared build && npm run -w apps/api build && npm run -w apps/web build",
    "dev": "concurrently -n API,WEB -c magenta,cyan \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm run -w apps/api dev",
    "dev:web": "npm run -w apps/web dev",
    "typecheck": "npm run -w shared typecheck && npm run -w apps/api typecheck && npm run -w apps/web typecheck",
    "lint": "echo \"add eslint later\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.6.2"
  }
}
